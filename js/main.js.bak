/* ============================================================
   KANHOLIDAYS.COM — Main JavaScript
   Hero Slider, Trending Packages, Search, Mobile Nav, etc.
   ============================================================ */

// ── Package Data ──
const PACKAGES = [
  // International
  { id:1, name:"Island of Gods Explorer", dest:"Bali", category:"international", tags:["honeymoon","adventure"], duration:"5D4N", price:22576, originalPrice:28000, image:"https://images.unsplash.com/photo-1537996194471-e657df975ab4?w=500&q=80", rating:4.8, reviews:120, highlights:["Ubud","Seminyak","Kuta"], flights:true, hotel:true },
  { id:2, name:"Bali Honeymoon Bliss", dest:"Bali", category:"international", tags:["honeymoon"], duration:"6D5N", price:35000, originalPrice:42000, image:"https://images.unsplash.com/photo-1539367628448-4bc5c9d171c8?w=500&q=80", rating:4.9, reviews:85, highlights:["Private Villa","Spa","Sunset Dinner"], flights:true, hotel:true },
  { id:3, name:"Vietnam Highlights", dest:"Vietnam", category:"international", tags:["adventure"], duration:"6D5N", price:32743, originalPrice:40000, image:"https://images.unsplash.com/photo-1583417319070-4a69db38a482?w=500&q=80", rating:4.7, reviews:92, highlights:["Halong Bay","Hanoi","Ho Chi Minh"], flights:true, hotel:true },
  { id:4, name:"Dubai Dazzle", dest:"Dubai", category:"international", tags:[], duration:"5D4N", price:15302, originalPrice:20000, image:"https://images.unsplash.com/photo-1512453979798-5ea266f8880c?w=500&q=80", rating:4.8, reviews:200, highlights:["Burj Khalifa","Desert Safari","Mall"], flights:false, hotel:true },
  { id:5, name:"Japan Cherry Blossom", dest:"Japan", category:"international", tags:["adventure"], duration:"7D6N", price:37251, originalPrice:45000, image:"https://images.unsplash.com/photo-1493976040374-85c8e12f0c0e?w=500&q=80", rating:4.9, reviews:64, highlights:["Tokyo","Kyoto","Osaka"], flights:true, hotel:true },
  { id:6, name:"Coral Paradise", dest:"Maldives", category:"international", tags:["honeymoon"], duration:"5D4N", price:72250, originalPrice:90000, image:"https://images.unsplash.com/photo-1514282401047-d79a71a590e8?w=500&q=80", rating:4.9, reviews:150, highlights:["Overwater Villa","Snorkeling","Spa"], flights:true, hotel:true },
  { id:7, name:"Bangkok & Pattaya", dest:"Thailand", category:"international", tags:["adventure"], duration:"5D4N", price:19999, originalPrice:25000, image:"https://images.unsplash.com/photo-1528181304800-259b08848526?w=500&q=80", rating:4.6, reviews:180, highlights:["Grand Palace","Pattaya Beach","Nightlife"], flights:false, hotel:true },
  { id:8, name:"Tropical Bliss", dest:"Mauritius", category:"international", tags:["honeymoon"], duration:"5D4N", price:54225, originalPrice:65000, image:"https://images.unsplash.com/photo-1578662996442-48f60103fc96?w=500&q=80", rating:4.8, reviews:95, highlights:["Beach","Snorkeling","Chamarel"], flights:true, hotel:true },
  { id:9, name:"Best of Europe", dest:"Europe", category:"international", tags:[], duration:"10D9N", price:44888, originalPrice:55000, image:"https://images.unsplash.com/photo-1499856871958-5b9627545d1a?w=500&q=80", rating:4.7, reviews:73, highlights:["Paris","Swiss Alps","Rome"], flights:true, hotel:true },
  { id:10, name:"Tbilisi & Beyond", dest:"Georgia", category:"international", tags:["adventure"], duration:"6D5N", price:27703, originalPrice:35000, image:"https://images.unsplash.com/photo-1565008576549-57569a49371d?w=500&q=80", rating:4.6, reviews:45, highlights:["Tbilisi","Kazbegi","Wine Country"], flights:true, hotel:true },
  { id:11, name:"Lanka Heritage", dest:"Sri Lanka", category:"international", tags:["adventure"], duration:"7D6N", price:39916, originalPrice:48000, image:"https://images.unsplash.com/photo-1586523969098-a8b8a5a3d2e1?w=500&q=80", rating:4.7, reviews:58, highlights:["Sigiriya","Kandy","Galle"], flights:true, hotel:true },
  { id:12, name:"Singapore Express", dest:"Singapore", category:"international", tags:[], duration:"4D3N", price:25000, originalPrice:32000, image:"https://images.unsplash.com/photo-1525625293386-3f8f99389edd?w=500&q=80", rating:4.7, reviews:110, highlights:["Marina Bay","Sentosa","Gardens"], flights:false, hotel:true },

  // India
  { id:20, name:"Paradise Explorer", dest:"Kashmir", category:"india", tags:["adventure"], duration:"5D4N", price:16134, originalPrice:22000, image:"https://images.unsplash.com/photo-1564507592263-b7a4312f6a77?w=500&q=80", rating:4.9, reviews:210, highlights:["Dal Lake","Gulmarg","Pahalgam"], flights:false, hotel:true },
  { id:21, name:"Royal Rajasthan", dest:"Rajasthan", category:"india", tags:[], duration:"6D5N", price:12999, originalPrice:18000, image:"https://images.unsplash.com/photo-1477587458883-47145ed94245?w=500&q=80", rating:4.7, reviews:95, highlights:["Jaipur","Udaipur","Jodhpur"], flights:false, hotel:true },
  { id:22, name:"Leh Ladakh Ride", dest:"Ladakh", category:"india", tags:["adventure"], duration:"7D6N", price:11394, originalPrice:16000, image:"https://images.unsplash.com/photo-1626621341517-bbf3d9990a23?w=500&q=80", rating:4.8, reviews:175, highlights:["Pangong","Nubra","Khardung La"], flights:false, hotel:true },
  { id:23, name:"Scotland of East", dest:"Meghalaya", category:"india", tags:["adventure"], duration:"5D4N", price:16794, originalPrice:22000, image:"https://images.unsplash.com/photo-1598091383021-15ddea10925d?w=500&q=80", rating:4.6, reviews:55, highlights:["Living Root Bridge","Dawki","Cherrapunji"], flights:false, hotel:true },
  { id:24, name:"God's Own Country", dest:"Kerala", category:"india", tags:["honeymoon"], duration:"6D5N", price:14202, originalPrice:19000, image:"https://images.unsplash.com/photo-1602216056096-3b40cc0c9944?w=500&q=80", rating:4.8, reviews:130, highlights:["Alleppey","Munnar","Kochi"], flights:false, hotel:true },
  { id:25, name:"Beach Vibes", dest:"Goa", category:"india", tags:["adventure"], duration:"4D3N", price:8999, originalPrice:12000, image:"https://images.unsplash.com/photo-1512343879784-a960bf40e7f2?w=500&q=80", rating:4.5, reviews:220, highlights:["Beaches","Nightlife","Water Sports"], flights:false, hotel:true },
  { id:26, name:"Himalayan Escape", dest:"Sikkim", category:"india", tags:["adventure"], duration:"6D5N", price:11935, originalPrice:16000, image:"https://images.unsplash.com/photo-1626621341517-bbf3d9990a23?w=500&q=80", rating:4.7, reviews:65, highlights:["Gangtok","Nathula","Pelling"], flights:false, hotel:true },
  { id:27, name:"Spiti Valley Road Trip", dest:"Spiti", category:"india", tags:["adventure"], duration:"8D7N", price:15000, originalPrice:20000, image:"https://images.unsplash.com/photo-1585409677983-0f6c41ca9c3b?w=500&q=80", rating:4.8, reviews:70, highlights:["Key Monastery","Chandrataal","Pin Valley"], flights:false, hotel:true },
  { id:28, name:"Manali & Shimla", dest:"Himachal Pradesh", category:"india", tags:["honeymoon"], duration:"5D4N", price:9999, originalPrice:14000, image:"https://images.unsplash.com/photo-1626621341517-bbf3d9990a23?w=500&q=80", rating:4.6, reviews:190, highlights:["Solang Valley","Mall Road","Rohtang"], flights:false, hotel:true },
];

// ── Hero Slider Data ──
const HERO_SLIDES = [
  { title: "Discover Bali", subtitle: "Island of the Gods — from ₹22,576 per person" },
  { title: "Experience Dubai", subtitle: "City of Dreams — from ₹15,302 per person" },
  { title: "Explore Vietnam", subtitle: "Land of Emerald Waters — from ₹32,743 per person" },
  { title: "Magical Kashmir", subtitle: "Paradise on Earth — from ₹16,134 per person" },
  { title: "Dream Maldives", subtitle: "Pearl of the Indian Ocean — from ₹72,250 per person" },
];

// ── DOM Ready ──
document.addEventListener('DOMContentLoaded', () => {
  initHeroSlider();
  initHeader();
  initMobileMenu();
  initSearch();
  initTrendingPackages();
  initScrollReveal();
  initCallbackModal();
});


// ── Hero Slider ──
function initHeroSlider() {
  const slides = document.querySelectorAll('.hero-slide');
  const titleEl = document.getElementById('heroTitle');
  const subtitleEl = document.getElementById('heroSubtitle');
  const progressBar = document.getElementById('heroProgressBar');
  if (!slides.length) return;

  let current = 0;
  const duration = 3500; // 3.5s per slide

  function animateProgress() {
    if (!progressBar) return;
    progressBar.style.transition = 'none';
    progressBar.style.width = '0%';
    requestAnimationFrame(() => {
      requestAnimationFrame(() => {
        progressBar.style.transition = `width ${duration}ms linear`;
        progressBar.style.width = '100%';
      });
    });
  }

  function goToSlide(index) {
    slides[current].classList.remove('active');
    current = index % slides.length;
    slides[current].classList.add('active');

    if (titleEl && HERO_SLIDES[current]) {
      titleEl.style.opacity = 0;
      titleEl.style.transform = 'translateY(20px)';
      subtitleEl.style.opacity = 0;
      subtitleEl.style.transform = 'translateY(20px)';

      setTimeout(() => {
        titleEl.textContent = HERO_SLIDES[current].title;
        subtitleEl.textContent = HERO_SLIDES[current].subtitle;
        titleEl.style.transition = 'opacity 0.6s, transform 0.6s';
        subtitleEl.style.transition = 'opacity 0.6s 0.15s, transform 0.6s 0.15s';
        titleEl.style.opacity = 1;
        titleEl.style.transform = 'translateY(0)';
        subtitleEl.style.opacity = 1;
        subtitleEl.style.transform = 'translateY(0)';
      }, 300);
    }
    animateProgress();
  }

  animateProgress();
  setInterval(() => goToSlide(current + 1), duration);
}


// ── Header Scroll ──
function initHeader() {
  const header = document.getElementById('siteHeader');
  if (!header) return;

  let lastScroll = 0;
  window.addEventListener('scroll', () => {
    const scroll = window.scrollY;
    header.classList.toggle('scrolled', scroll > 50);
    lastScroll = scroll;
  }, { passive: true });
}


// ── Mobile Menu ──
function initMobileMenu() {
  const toggle = document.getElementById('mobileToggle');
  const overlay = document.getElementById('mobileOverlay');
  const menu = document.getElementById('mobileMenu');
  if (!toggle || !menu) return;

  function openMenu() {
    toggle.classList.add('active');
    menu.classList.add('active');
    if (overlay) overlay.classList.add('active');
    document.body.classList.add('no-scroll');
  }

  function closeMenu() {
    toggle.classList.remove('active');
    menu.classList.remove('active');
    if (overlay) overlay.classList.remove('active');
    document.body.classList.remove('no-scroll');
  }

  toggle.addEventListener('click', () => {
    toggle.classList.contains('active') ? closeMenu() : openMenu();
  });

  if (overlay) overlay.addEventListener('click', closeMenu);
}


// ── Search Overlay ──
function initSearch() {
  const searchToggle = document.getElementById('searchToggle');
  const searchOverlay = document.getElementById('searchOverlay');
  const searchClose = document.getElementById('searchClose');
  const searchInput = document.getElementById('searchInput');
  const heroSearchInput = document.getElementById('heroSearchInput');
  const heroSearchBtn = document.getElementById('heroSearchBtn');

  if (searchToggle && searchOverlay) {
    searchToggle.addEventListener('click', () => {
      searchOverlay.classList.add('active');
      setTimeout(() => searchInput?.focus(), 300);
    });
  }

  if (searchClose && searchOverlay) {
    searchClose.addEventListener('click', () => {
      searchOverlay.classList.remove('active');
    });
  }

  // Escape key closes search
  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape' && searchOverlay?.classList.contains('active')) {
      searchOverlay.classList.remove('active');
    }
  });

  // Hero search bar click opens overlay
  if (heroSearchInput) {
    heroSearchInput.addEventListener('focus', () => {
      if (searchOverlay) {
        searchOverlay.classList.add('active');
        setTimeout(() => searchInput?.focus(), 300);
      }
    });
  }
}


// ── Trending Packages ──
function initTrendingPackages() {
  const grid = document.getElementById('trendingGrid');
  const tabs = document.querySelectorAll('.trending-tab');
  if (!grid) return;

  function renderPackages(filter = 'all') {
    let filtered = PACKAGES;
    if (filter === 'international') filtered = PACKAGES.filter(p => p.category === 'international');
    else if (filter === 'india') filtered = PACKAGES.filter(p => p.category === 'india');
    else if (filter === 'honeymoon') filtered = PACKAGES.filter(p => p.tags.includes('honeymoon'));
    else if (filter === 'adventure') filtered = PACKAGES.filter(p => p.tags.includes('adventure'));

    // Show max 9
    filtered = filtered.slice(0, 9);

    grid.innerHTML = filtered.map(pkg => `
      <div class="card" onclick="location.href='/packages/${pkg.name.toLowerCase().replace(/[^a-z0-9]+/g,'-')}'">
        <div class="card__image">
          <img src="${pkg.image}" alt="${pkg.name}" loading="lazy">
          <span class="card__badge">${pkg.dest}</span>
          <button class="card__wishlist" onclick="event.stopPropagation();this.classList.toggle('active')" aria-label="Add to wishlist">
            <i class="fas fa-heart"></i>
          </button>
        </div>
        <div class="card__body">
          <div class="card__rating">
            <span class="stars">${'★'.repeat(Math.floor(pkg.rating))}${pkg.rating % 1 ? '½' : ''}</span>
            <span>${pkg.rating} (${pkg.reviews} reviews)</span>
          </div>
          <h3 class="card__title">${pkg.name}</h3>
          <p class="card__subtitle">${pkg.highlights.join(' · ')}</p>
          <div class="card__chips">
            <span class="chip"><i class="fas fa-clock"></i> ${pkg.duration}</span>
            ${pkg.flights ? '<span class="chip"><i class="fas fa-plane"></i> Flights Inc.</span>' : ''}
            <span class="chip"><i class="fas fa-hotel"></i> Hotel</span>
          </div>
          <div class="card__footer">
            <div class="card__price">
              <span class="card__price-original">₹${pkg.originalPrice.toLocaleString('en-IN')}</span>
              <span class="card__price-current">₹${pkg.price.toLocaleString('en-IN')}</span>
              <span class="card__price-label">per person</span>
            </div>
            <span class="btn btn--sm btn--primary">View Details</span>
          </div>
        </div>
      </div>
    `).join('');
  }

  // Initial render
  renderPackages('all');

  // Tab click handlers
  tabs.forEach(tab => {
    tab.addEventListener('click', () => {
      tabs.forEach(t => t.classList.remove('active'));
      tab.classList.add('active');
      renderPackages(tab.dataset.filter);
    });
  });
}


// ── Scroll Reveal ──
function initScrollReveal() {
  const reveals = document.querySelectorAll('.reveal, .reveal-left, .reveal-right, .reveal-scale');
  if (!reveals.length) return;

  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('visible');
        observer.unobserve(entry.target);
      }
    });
  }, {
    threshold: 0.1,
    rootMargin: '0px 0px -60px 0px'
  });

  reveals.forEach(el => observer.observe(el));
}


// ── Callback Modal ──
function initCallbackModal() {
  const modal = document.getElementById('callbackModal');
  if (!modal) return;

  // Close on overlay click
  modal.addEventListener('click', (e) => {
    if (e.target === modal) closeCallbackModal();
  });

  // Escape key
  document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape' && modal.classList.contains('active')) {
      closeCallbackModal();
    }
  });
}

function openCallbackModal(pkgId, pkgName) {
  const modal = document.getElementById('callbackModal');
  const destSelect = document.getElementById('callbackDest');
  if (modal) {
    modal.classList.add('active');
    document.body.classList.add('no-scroll');
    if (destSelect && pkgName) {
      // Try to match destination
      for (let opt of destSelect.options) {
        if (pkgName.toLowerCase().includes(opt.value.toLowerCase())) {
          opt.selected = true;
          break;
        }
      }
    }
  }
}

function closeCallbackModal() {
  const modal = document.getElementById('callbackModal');
  if (modal) {
    modal.classList.remove('active');
    document.body.classList.remove('no-scroll');
  }
}

// Make functions globally accessible
window.openCallbackModal = openCallbackModal;
window.closeCallbackModal = closeCallbackModal;
